This web application is based on Flask framework (python based). The web-app is used to classify leaves based on a trained model.

The web-app can be run on any system just by installing the required version of flask and running the app on localhost.